# https://docs.railway.app/guides/nixpacks

[build]
provider = "nixpacks"

[build.nixpacks]
python = "3.12"

[[build.nixpacks.apt]]
packages = ["postgresql-client"]

[start]
cmd = "uvicorn fastapi_service.main:app --host 0.0.0.0 --port $PORT"
